services:
  - type: web
    name: emotion-dj
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn predict:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1 --preload
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
